# Swapping two variables.
a, b = 5, 10
a, b = b, a
print(a,b)

# Counting characters in a string.
# Assigning result of multiple operationson a string to variable in one line.

word = 'python'
length = len(word)
print(f"Length of the word: {length}")